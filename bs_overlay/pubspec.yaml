name: bs_overlay
description: "Optional-Context Smart Overlay that is prioritized and packed with convenience widgets"
version: 0.0.1
homepage: https://bsaeid.dev/
#repository: https://github.com/B-Saeid/bs_overlay
#issue_tracker: https://github.com/ueman/bs_overlay/issues
publish_to: none

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  bs_ref_query:
    git:
      url: https://github.com/B-Saeid/bs_library.git
      path: bs_ref_query
  bs_utils:
    git:
      url: https://github.com/B-Saeid/bs_library.git
      path: bs_utils
  bs_styles:
    git:
      url: https://github.com/B-Saeid/bs_library.git
      path: bs_styles
  bs_widgets:
    git:
      url: https://github.com/B-Saeid/bs_library.git
      path: bs_widgets
  flutter:
    sdk: flutter
  flutter_riverpod: 3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
