import 'package:flutter/material.dart';

abstract final class FeedbackC {
  static const drawColors = [
    Colors.black,
    Colors.red,
    Colors.green,
    Colors.blue,
  ];

  static const double defaultPadding = 8.0;
  // static List<Color> defaultColors = Colors.primaries;
}
